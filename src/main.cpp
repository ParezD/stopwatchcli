#include "BigNum.h"
#include <iostream>

using namespace std;

int main(int argc, char* argv[]){

cout << "Hello world! cunt!" << endl;
sayhi();


for(int i = 0; i < nsize[0]; ++i){
  for(int j = 0; j < nsize[1]; ++j)
    //cout << two[i/height][j/width] << space[j/width] << two[i/height][j/width];
    cout << two[i/height][j/width];
  //for(int j = 0; j < nsize[1]; ++j)
  //  cout << space[j/9];
  for(int j = 0; j < width; ++j)
    cout << sp;
  for(int j = 0; j < nsize[1]; ++j)
    cout << two[i/height][j/width];
  cout << endl;
}

cout << none << endl;

return EXIT_SUCCESS;
}
